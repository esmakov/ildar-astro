---
const { imageList, alt } = Astro.props;
---

<div class="flex flex-col">
  {
    imageList.map((imageUrl) => (
      <img
        src={imageUrl}
        class="max-w-s md:max-w-3xl lg:max-w-4xl"
        alt={alt}
        loading="lazy"
      />
    ))
  }
</div>
